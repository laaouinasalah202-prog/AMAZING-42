[project]
name = "maze-gen"
version = "0.1.0"
description = ""
authors = [
    {name = "zboualam. slaaouin"}
]
requires-python = ">=3.10"
dependencies = [
    "mypy (>=1.19.1,<2.0.0)",
    "flake8 (>=7.3.0,<8.0.0)",
    "cleanpy (>=0.5.1,<0.6.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
